[package]
name = "wealth_proof_eth"
type = "bin"
authors = [""]
compiler_version = ">=1.0.0"

[dependencies]
# noir-trie-proofs: provides TrieProof struct + verify_state_root for Ethereum MPT verification
trie = { path = "../../../noir-trie-proofs/lib" }
keccak = { tag = "v0.1.3", git = "https://github.com/noir-lang/keccak256" }